apiVersion: saw.redhat.com/v1alpha1
kind: Providers
metadata:
  profile: data-science
spec:
  providers:
    - name: custom
      type: custom
      nemoclawProvider: openrouter
      credentialSecret: inference
      credentialSecretKey: api_key
      urlSecretKey: url
