# Per-user sandbox configuration.
# Only the owner is required — all other values use defaults from values.yaml.
# Override inference.provider/model here if the user needs a different provider.
accessControl:
  owner: alice
